export default async function decorate(block) {
  // The block structure is already correct:
  // Each row contains cells, each cell has a value (strong) and description
  // No additional DOM manipulation needed
}
